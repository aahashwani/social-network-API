const { Users, Thoughts } = require("../models");

const createUser = (req, res) => { return new User(req, res || {}, req. authenticated = false); };

module.exports = 

getUser(req, res) 
    Users.find()

const userControllers = {
    createUser: ({body}, callback)
}

